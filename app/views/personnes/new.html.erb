<% provide(:title, 'Creation Personnage') %>
<h1>Creation Personnage</h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for(@personne) do |f| %>
        <%= render 'shared/error_messages', object: f.object %>

        <%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.submit "Ajouter Personnage", class: "btn btn-large btn-primary" %>
     <% end %>
   </div>
</div>
